/*******************************
         Site Overrides
*******************************/

.pushable:not(body) {
  transform: inherit;
}

.pushable:not(body) > .ui.sidebar,
.pushable:not(body) > .fixed,
.pushable:not(body) > .pusher:after {
  position: fixed;
}
